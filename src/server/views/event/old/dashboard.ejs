<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <!-- IMPORTANT #1 Start -->
    <script src="/auth/js/getAuthUserInfo.js"></script>
    <script src="/event/js/dashboard.js"></script>
    <!-- IMPORTANT #1 End -->
    <title>Document</title>
  </head>
  <body class="container p-5">
    <%- include('../_partials/logout.ejs'); %> <%-
    include('../_partials/breadcrumb.ejs') %>
    <h3>Event Dashboard</h3>
    <!-- IMPORTANT #2 Start -->
    <h1 id="event-id" style="display: none"><%= eventId %></h1>
    <!-- IMPORTANT #2 End -->
    <h1><%= eventName %></h1>
    <a href="/event/<%= eventId %>/department/new" class="btn btn-success">
      Create Department
    </a>
    <!-- IMPORTANT #3 Start (department list MUST be id="main-list") -->
    <div id="main-list" class="container mt-2">
      <!-- IMPORTANT #3 End -->
      <h2>Departments</h2>
      <!-- Item Design Start -->
      <!--
      <a href="">
        <button class="btn btn-danger delete-department-btn">
          Delete Department
        </button>
        <div class="row mt-2">
          <div class="col">
            <h3>Department Name</h3>
          </div>
        </div>
      </a>
      -->
      <!-- Item Design End -->
      <!-- TODO: Change to Fetch -->
      <!--
      <% departments.forEach(department => { %>
      <div class="row mt-2">
        <div class="col d-flex">
          <a
            href="/event/<%= eventId %>/department/<%= department._id %>/edit"
            class="btn btn-primary me-2"
            >Edit Department</a
          >
          <a
            href="/event/<%= eventId %>/department/<%= department._id %>/dashboard"
            ><p><%= department.name %></p></a
          >
        </div>
      </div>
      <% }) %>
      -->
    </div>

    <div class="container mt-2" id="admin-credentials-sect">
      <h2>Admin Credentials</h2>
      <a href="/event/<%= eventId %>/admin/edit" class="btn btn-primary"
        >Edit Admin Credentials</a
      >
      <table class="table">
        <thead>
          <tr>
            <th>Username</th>
            <th>Password</th>
          </tr>
        </thead>
        <!-- IMPORTANT #4 Start (list MUST have id="admin-credentials-list") -->
        <tbody id="admin-credentials-list">
          <!-- IMPORTANT #4 End -->
          <!--
          <tr>
            <td><%= adminUser.username %></td>
            <td><%= adminUser.password %></td>
          </tr>
          -->
        </tbody>
      </table>
    </div>
  </body>
</html>
