<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <!-- IMPORTANT #1 Start -->
    <script src="/file/auth/js/getAuthUserInfo.js"></script>
    <script src="/file/event/js/editDepartment.js"></script>
    <link rel="stylesheet" href="/file/_partials/skeleton.css" />
    <!-- IMPORTANT #1 End -->
  </head>
  <body class="container p-5 d-flex justify-content-center align-items-center">
    <main class="container w-25">
      <div class="container">
        <h1>Edit <%= department.name %></h1>
        <!-- IMPORTANT #2 Start -->
        <h1 id="event-id" style="display: none"><%= event._id %></h1>
        <h1 id="department-id" style="display: none"><%= department._id %></h1>
        <!-- IMPORTANT #2 End -->
      </div>
      <!-- IMPORTANT #3 Start (form MUST have id="main-form") -->
      <form action="" id="main-form" method="post">
        <!-- IMPORTANT #3 End -->
        <div class="form-floating">
          <!-- IMPORTANT #4 Start -->
          <input type="hidden" name="eventId" value="<%= event._id %>" />
          <input
            type="hidden"
            name="departmentId"
            value="<%= department._id %>"
          />
          <!-- IMPORTANT #4 End -->
          <input
            type="text"
            name="name"
            id="input-name"
            class="form-control"
            placeholder="Department Name"
            value="<%= department.name %>"
            required
          />
          <label for="input-name" class="form-label">Department Name</label>
        </div>

        <div
          class="container d-flex justify-content-center align-items-center mt-3"
        >
          <!-- IMPORTANT #5 Start (submit button MUST have id="submit-btn") -->
          <button type="button" id="submit-btn" class="btn btn-primary">
            Edit Department
          </button>
          <!-- IMPORTANT #5 End -->
        </div>
      </form>
    </main>
  </body>
</html>
