<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <!-- IMPORTANT #1 Start -->
    <script src="/auth/js/getAuthUserInfo.js"></script>
    <script src="/department/js/dashboard.js"></script>
    <!-- IMPORTANT #1 End -->
    <title>Document</title>
  </head>
  <body class="container p-5">
    <%- include('../_partials/logout.ejs'); %> <%-
    include('../_partials/breadcrumb.ejs') %>
    <h4><%= event.name %></h4>
    <h1><%= department.name %></h1>

    <!-- IMPORTANT #2 Start -->
    <p id="event-id" class="d-none"><%= event._id %></p>
    <p id="department-id" class="d-none"><%= department._id %></p>
    <!-- IMPORTANT #2 End -->

    <div class="container" id="action-buttons-sect" style="display: none">
      <!-- Invisible to User Access_Level Roles -->
      <!--
    <a
      href="/event/<%= event._id %>/department/<%= department._id %>/form/new"
      id="add-form-btn"
      class="btn btn-success"
      >Add Form</a
    >
    <a
      href="/event/<%= event._id %>/department/<%= department._id %>/users/new"
      class="btn btn-secondary"
      >Add Department Members</a
    >
    --></div>

    <div id="forms-sect" class="container mt-2">
      <h2>Forms</h2>
      <!-- IMPORTANT #3 Start (forms MUST have id="forms-list") -->
      <div class="container" id="forms-list">
        <!-- IMPORTANT #3 End -->

        <!-- Item Design Start -->
        <!--
        <% forms.forEach(form => { %>
        <div class="row mt-2">
          <div class="col d-flex">
            <button class="btn btn-danger delete-form-btn">Delete Form</a>
            <a
              href="/event/<%= event._id %>/department/<%= department._id %>/form/<%= form._id %>"
            >
              <p><%= form.name %></p>
            </a>
          </div>
        </div>
        <% }); %>
        -->
        <!-- Item Design End -->
      </div>
    </div>

    <!-- IMPORTANT #4 Start (admin credentials section MUST have id="admin-credentials-sect") -->
    <div class="container mt-2" id="admin-credentials-sect">
      <!-- IMPORTANT #4 End -->

      <!-- TODO: Change to Fetch -->
      <!--
      <h2>Admin Credentials</h2>
      <a
        href="/event/<%= event._id %>/department/<%= department._id %>/admin/edit"
        class="btn btn-primary"
        >Change Admin Credentials</a
      >
      <table class="table">
        <thead>
          <tr>
            <th>Username</th>
            <th>Password</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= adminUser.username %></td>
            <td><%= adminUser.password %></td>
          </tr>
        </tbody>
      </table>
      -->
    </div>

    <div class="container" id="members-sect">
      <h2>Members</h2>
      <!-- IMPORTANT #5 Start (member credentials list MUST have id="members-list") -->
      <div class="container" id="members-list">
        <!-- IMPORTANT #5 End -->

        <!-- Item Design Start -->
        <!-- TODO: Change to Fetch -->
        <!--
        <% users.forEach(user => {%>
        <div class="row mt-2">
          <div class="col d-flex">
            <a
              href="/event/<%= event._id %>/department/<%= department._id %>/users/<%= user._id %>"
              class="btn btn-primary ms-2"
              >Edit User</a
            >
            <p class="ms-2"><%= user.username %></p>
          </div>
        </div>
        <%}); %>
        -->
        <!-- Item Design End -->
      </div>
    </div>
  </body>
</html>
