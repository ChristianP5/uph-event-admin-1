<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UPH Survey Hub</title>
    <link rel="stylesheet" href="/file/department/css/styles.css">

    <!-- IMPORTANT #1 Start -->
    <script src="/file/auth/js/getAuthUserInfo.js"></script>
    <script src="/file/department/js/dashboard.js"></script>
    <link rel="stylesheet" href="/file/_partials/css/skeleton.css">
    <!-- IMPORTANT #1 End -->

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inria+Sans:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="/file/_partials/images/favicon.ico">
</head>
<body class="bg-gradient-to-b from-white to-blue-100 min-h-screen flex flex-col" style="font-family: Inter, sans-serif">

    <%- include('../_partials/navbar.ejs') %>

    <!-- Main Content area -->
    <main id="main-content" class="main-content mt-10 transition-all duration-300">
        <div class="content flex-1 text-[#003366] overflow-auto">
            <!-- Current Login Start -->
            <%- include('../_partials/currentLogin.ejs') %>
            <!-- Current Login End -->
            
            <!-- Breadcrumbs Start -->
            <%- include('../_partials/breadcrumb.ejs') %>
            <!-- Breadcurmbs End -->

            <div class="header-text font-bold pt-2 mb-5">
              <h4><%= event.name %></h4>
            </div>
            <div class="header-text font-bold pt-2">
              <h1><%= department.name %></h1>
            </div><br>

            <!-- IMPORTANT #2 Start -->
            <p id="event-id" style="display: none;"><%= event._id %></p>
            <p id="department-id" style="display: none;"><%= department._id %></p>
            <!-- IMPORTANT #2 End -->
            
            <!-- Edit Content area -->
            <div class="font-bold text-xl flex items-center justify-between">
                <div class="flex items-center">
                    <i class="fas fa-file-alt mr-3"></i> Forms
                </div>
            </div>

            <!-- ADMIN DISPLAY #1 Start -->
            <div id="form-action-buttons-sect" class="mt-2 text-center">
                <!--
                <button id="add-form-btn" class="bg-yellow-500 text-white px-2 py-1 rounded flex items-center space-x-1" onclick="location.href='/event/<%= event._id %>/department/<%= department._id %>/form/new'">
                    <i class="fas fa-plus"></i>
                    <span class="text-sm"> Create Form</span>
                </button>
                -->
            </div>
            <!-- ADMIN DISPLAY #1 End -->
            <br>

            <div class="rounded-lg overflow-auto">
                <table class="min-w-full">
                    <tbody id="forms-list">
                      <!-- Item Design Start -->
                       <!--
                        <tr class="border-t border-[#AAC1D7] hover:bg-[#E5EDF4] hover:shadow-lg">
                            <td class="py-3 px-4">
                                <div class="event-name-wrapper">
                                    <a href="ea-forms.html" class="text-blue-600 underline event-name">ACS2024_Informatics</a>
                                </div>
                                <div class="event-buttons mt-2 flex space-x-2">
                                    <button class="bg-blue-500 text-white px-2 py-1 rounded flex items-center space-x-1" onclick="location.href='ea-forms.html'">
                                        <i class="fas fa-edit"></i>
                                        <span class="text-sm">Edit Form</span>
                                    </button>
                                    <button class="bg-red-500 text-white px-2 py-1 rounded flex items-center space-x-1" onclick="confirmDeleteForm('')">
                                        <i class="fas fa-trash"></i>
                                        <span class="text-sm">Delete Form</span>
                                    </button>
                                </div>
                            </td>
                        </tr>
                        -->
                        <!-- Item Design End -->

                        <!-- Skeleton Loading Placeholder Start -->
                       
                        <tr class="border-t border-[#AAC1D7] hover:bg-[#E5EDF4] hover:shadow-lg">
                            <td class="py-3 px-4">
                                <div class="event-name-wrapper">
                                    <p class="skeleton skeleton-text"></p>
                                </div>
                            </td>
                        </tr>
                        
                        <!-- Skeleton Loading Placeholder End -->

                    </tbody>
                </table>
            </div>

            <!-- ADMIN DISPLAY #2 Start -->
            <!-- Edit Department Admin credentials area -->
            <section id="admin-credentials-sect-sect">
                <!--
            <div class="mt-10 font-bold text-xl flex items-center">
                <i class="fas fa-user-friends mr-3"></i> Edit Department Admin Credentials
            </div>
            
            <div class="rounded-lg overflow-auto">
                <table class="min-w-full">
                    <thead>
                        <tr>
                            <th class="py-2 px-4 text-left">Username</th>
                            <th class="py-2 px-4 text-left">Password</th>
                            <th class="py-2 px-4 text-left">Action</th>
                        </tr>
                    </thead>
                    <tbody id="admin-credentials-sect">
                        -->
                      <!-- Item Design Start -->
                       <!--
                        <tr class="border-t border-[#AAC1D7] hover:bg-[#E5EDF4] hover:shadow-lg">
                            <td class="py-3 px-4">Christian</td>
                            <td class="py-3 px-4">
                                <button class="bg-blue-500 text-white px-2 py-1 rounded" onclick="toggleEditCredentials('Christian', this)">Edit User Credentials</button>
                            </td>
                        </tr>
                        -->
                        <!-- Item Design End -->
                         <!--
                    </tbody>
                </table>
            </div>
            -->
        </section>
            <!-- ADMIN DISPLAY #2 End -->

            <!-- Edit credentials area -->
            <div class="mt-10 font-bold text-xl flex items-center">
                <i class="fas fa-user-friends mr-3"></i> Edit Department User Credentials
            </div>
            <div id="members-action-buttons-sect" class="mt-2 text-center">
                <!-- ADMIN DISPLAY #3 Start -->
                 <!--
              <button id="add-member-btn" class="bg-yellow-500 text-white px-2 py-1 rounded flex items-center space-x-1" onclick="location.href='/event/<%= event._id %>/department/<%= department._id %>/users/new'">
                  <i class="fas fa-plus"></i>
                  <span class="text-sm"> Add Department Member</span>
              </button>
              -->
              <!-- ADMIN DISPLAY #3 End -->
          </div><br>
            <div class="rounded-lg overflow-auto">
                <table class="min-w-full">
                    <thead>
                        <tr>
                            <th class="py-2 px-4 text-left">Username</th>
                            <th class="py-2 px-4 text-left">Action</th>
                        </tr>
                    </thead>
                    <tbody id="members-list">
                      <!-- Item Design Start -->
                       <!--
                        <tr class="border-t border-[#AAC1D7] hover:bg-[#E5EDF4] hover:shadow-lg">
                            <td class="py-3 px-4">Christian</td>
                            <td class="py-3 px-4">
                                <button class="bg-blue-500 text-white px-2 py-1 rounded" onclick="toggleEditCredentials('Christian', this)">Edit User Credentials</button>
                            </td>
                        </tr>
                        -->
                        <!-- Item Design End -->

                        <!-- Skeleton Loading Placeholder Start -->
                       
                        <tr class="border-t border-[#AAC1D7] hover:bg-[#E5EDF4] hover:shadow-lg">
                            <td class="py-3 px-4">
                                <p class="skeleton skeleton-text"></p>
                            </td>
                            <td class="py-3 px-4">
                                <p class="skeleton skeleton-text"></p>
                            </td>
                        </tr>
                        
                        <!-- Skeleton Loading Placeholder End -->
                    </tbody>
                </table>
            </div>

            <!-- For Edit Member Credentials -->
            <div id="editMemberCredentialsContainer" class="mt-4 hidden">
                <form id="edit-dep-member-creds-form" action="">
                    <div class="bg-gray-100 p-4 rounded">
                        <div class="mb-4">
                            <label for="newUsername" class="block text-sm font-medium text-gray-700">New Username:</label>
                            <input name="username" type="text" id="newUsername" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="Enter new username" required>
                        </div>
                        <div class="mb-4">
                            <label for="newPassword" class="block text-sm font-medium text-gray-700">New Password:</label>
                            <input name="password" type="password" id="newPassword" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="Enter new password" required>
                        </div>
                        <div class="flex justify-end space-x-2">
                            <button id="submit-btn" type="button" class="bg-blue-500 text-white px-4 py-2 rounded">Save Changes</button>
                            <button type="button" class="bg-red-500 text-white px-4 py-2 rounded" onclick="cancelMemberEdit()">Cancel</button>
                        </div>
                    </div>
                </form>
            </div>

            <!-- For Edit Admin Credentials -->
            <div id="editAdminCredentialsContainer" class="mt-4 hidden">
                <form id="edit-dep-admin-creds-form" action="">
                    <div class="bg-gray-100 p-4 rounded">
                        <div class="mb-4">
                            <label for="newUsername" class="block text-sm font-medium text-gray-700">New Username:</label>
                            <input name="username" type="text" id="newUsername" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="Enter new username" required>
                        </div>
                        <div class="mb-4">
                            <label for="newPassword" class="block text-sm font-medium text-gray-700">New Password:</label>
                            <input name="password" type="password" id="newPassword" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" placeholder="Enter new password" required>
                        </div>
                        <div class="flex justify-end space-x-2">
                            <button id="submit-btn" type="button" class="bg-blue-500 text-white px-4 py-2 rounded">Save Changes</button>
                            <button type="button" class="bg-red-500 text-white px-4 py-2 rounded" onclick="cancelEditAdmin()">Cancel</button>
                        </div>
                    </div>
                </form>
            </div>

    </main>

    <!-- Delete Form Popup -->
    <div class="popup-overlay" id="deletePopupOverlay" onclick="hideDeletePopup()"></div>
    <div class="delete-popup" id="deletePopup">
        <p>Are you sure you want to delete this form?</p>
        <button onclick="hideDeletePopup()">Cancel</button>
        <button id="confirmDeleteButton">Delete</button>
    </div>

    <!-- Delete Member Popup -->
    <div class="popup-overlay" id="deleteMemberPopupOverlay" onclick="hideDeletePopup()"></div>
    <div class="delete-popup" id="deleteMemberPopup">
        <p>Are you sure you want to delete this member?</p>
        <button onclick="hideDeleteMemberPopup()">Cancel</button>
        <button id="confirmDeleteMemberButton">Delete</button>
    </div>

    <!-- Additional scripts -->
    <script>
        function toggleDropdown(event) {
            event.preventDefault();
            event.target.parentElement.querySelector('.dropdown-content').classList.toggle('show');
            event.target.querySelector('.dropdown-arrow').classList.toggle('rotate');
        }

        function showLogoutPopup() {
            document.getElementById('logoutPopup').style.display = 'block';
            document.getElementById('popupOverlay').style.display = 'block';
        }

        function hideLogoutPopup() {
            document.getElementById('logoutPopup').style.display = 'none';
            document.getElementById('popupOverlay').style.display = 'none';
        }

        function toggleNavbar() {
            let x = document.getElementById("myNavbar");
            if (x.className === "navbar") {
                x.className += " responsive";
            } else {
                x.className = "navbar";
            }
        }

        /*
        function confirmDeleteForm(formId) {
            document.getElementById('deletePopupOverlay').style.display = 'block';
            document.getElementById('deletePopup').style.display = 'block';
            document.getElementById('confirmDeleteButton').onclick = function() {
                deleteForm(formId);
            };
        }
        */

        function hideDeletePopup() {
            document.getElementById('deletePopupOverlay').style.display = 'none';
            document.getElementById('deletePopup').style.display = 'none';
        }

        function hideDeleteMemberPopup() {
            document.getElementById('deleteMemberPopupOverlay').style.display = 'none';
            document.getElementById('deleteMemberPopup').style.display = 'none';
        }

        function deleteForm(formId) {
            console.log(`Deleting ${formId}`);
            // Add actual delete event logic here
            return true;
            hideDeletePopup();
        }

        function editForm(formName) {
            console.log(`Editing ${formName}`);
            // Add actual edit event logic here
        }

        /*
        function toggleEditCredentials(username, button) {
            const container = document.getElementById('editCredentialsContainer');
            const row = button.closest('tr');
            if (container.parentNode !== row) {
                container.remove();
                row.insertAdjacentElement('afterend', container);
            }
            container.classList.toggle('hidden');
            document.getElementById('newUsername').value = username;
        }
        */

        function saveCredentials() {
            const username = document.getElementById('newUsername').value.trim();
            const password = document.getElementById('newPassword').value.trim();

            if (username === '' || password === '') {
                alert('Both username and password are required.');
                return;
            }

            console.log('Credentials saved');
            document.getElementById('editCredentialsContainer').classList.add('hidden');
        }

        function cancelMemberEdit() {
            document.getElementById('editMemberCredentialsContainer').classList.add('hidden');
        }

        function cancelEditAdmin() {
            document.getElementById('editAdminCredentialsContainer').classList.add('hidden');
        }

    </script>
</body>
</html>
